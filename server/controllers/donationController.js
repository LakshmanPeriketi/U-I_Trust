// TODO (Donor - PRD 2): Implement all donation controller actions

// GET /api/donations
export const listDonations = async (_req, res) => {
  res.status(501).json({ message: 'Not implemented — PRD 2' });
};

// POST /api/donations
export const createDonation = async (_req, res) => {
  res.status(501).json({ message: 'Not implemented — PRD 2' });
};

// GET /api/donations/:id
export const getDonation = async (_req, res) => {
  res.status(501).json({ message: 'Not implemented — PRD 2' });
};

// PATCH /api/donations/:id
export const updateDonation = async (_req, res) => {
  res.status(501).json({ message: 'Not implemented — PRD 2' });
};

// DELETE /api/donations/:id
export const deleteDonation = async (_req, res) => {
  res.status(501).json({ message: 'Not implemented — PRD 2' });
};
