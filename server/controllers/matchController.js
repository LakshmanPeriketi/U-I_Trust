// TODO (Donor - PRD 2): Implement match controller actions

export const listMatches = async (_req, res) => {
  res.status(501).json({ message: 'Not implemented — PRD 2' });
};

export const createMatch = async (_req, res) => {
  res.status(501).json({ message: 'Not implemented — PRD 2' });
};

export const getMatch = async (_req, res) => {
  res.status(501).json({ message: 'Not implemented — PRD 2' });
};

export const updateMatchStatus = async (_req, res) => {
  res.status(501).json({ message: 'Not implemented — PRD 2' });
};
